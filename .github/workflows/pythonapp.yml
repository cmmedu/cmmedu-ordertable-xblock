name: Python application

on:
  push:
    branches:
      - 'cmm-release/*'
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - 'cmm-release/*'

jobs:
  test:
    uses: eol-uchile/workflow-commons/.github/workflows/pythonapp.yml@pythonapp-2
    with:
      
      django-lms: true
      django-cms: false
    